import {fileUpload} from '../../helpers/fileUpload'
describe('pruebas en file upload', () => {
  test('debe de cargar un archivo y retonar el url', async () => {
    // const resp = await fetch("http://cdn.brid.tv/live/partners/16291/snapshot/536725.jpg");
    // const blob = await resp.blob();
    // const file = new File([blob],"foto.png");
    // const url = await fileUpload( file );
    // expect( typeof url).toBe('string');
    // // borrar imagen de aqui usando cloudinary sdk

    expect(true).toBe(true);
  },20000)

})
